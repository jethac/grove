[package]
name = "grove-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "WebAssembly bindings for the grove procedural tree generator"

[lib]
crate-type = ["cdylib"]

[dependencies]
wasm-bindgen = "0.2"
grove-core = { path = "../grove-core" }
